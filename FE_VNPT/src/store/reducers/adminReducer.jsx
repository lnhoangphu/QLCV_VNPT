


const initialState = {
  isLoggedIn: false,
  userInfo:null,
};

const adminReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default adminReducer;